# Daily Log — 2026-03-20

### 09:56
- Diagnosed heartbeat silence: scheduled task exists (next run 2026-03-21 08:00), but last run (2026-03-18 08:00) failed with 401 OAuth expired. Auth recovered interactively since.
- Built `scripts/verify.py` — hard-fact system health check (auth, scheduled task, heartbeat log age, memory freshness, pending tasks). Exits 1 if issues found.
- Updated `/status` skill (`~/.claude/commands/status.md`) to run `verify.py` first as ground truth.
- Updated `claude_heartbeat.ps1`: on 401/auth failure, creates Todoist task via REST API instead of silently dying. Token moved from hardcoded to `.env` (gitignored).
- Added CLAUDE.md rule: never ask permission — just act unless action is destructive.
- Files touched: `scripts/verify.py` (new), `scripts/claude_heartbeat.ps1`, `~/.claude/commands/status.md`, `~/.claude/CLAUDE.md`, `.env` (new)
- Committed + pushed: `9c19d36`
- Key lesson: MCP not available in headless `claude -p` — use direct REST for notifications from scheduled tasks.
