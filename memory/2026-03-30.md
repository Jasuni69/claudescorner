# Daily Log — 2026-03-30

### 11:09

**Session: Infrastructure fixes + semantic memory upgrade**

**What was done:**
- Diagnosed broken Windows Task Scheduler tasks (`ClaudeWeekendBuild`, `ClaudeHeartbeat`) — both pointing to dead path `E:\2026\Claude's Corner\` after folder rename. Fixed both to `E:\2026\ClaudesCorner\`. Exit code `0xFFFD0000` = file not found.
- `ClaudesCorner-ContextPack` also had exit code 2 on last run — worth checking separately.
- Two `mcp__scheduled-tasks` entries (`weekend-build-saturday`, `weekend-build-sunday`) confirmed redundant — Task Scheduler is source of truth, those shells stay disabled.
- Added **SKILL NUDGE** rule to CLAUDE.md: after completing any loop task, self-assess whether a reusable pattern emerged and invoke `skill-creator` if yes.
- Upgraded `memory-mcp` `search_memory` from TF-IDF to **semantic embeddings** using `sentence-transformers` (`all-MiniLM-L6-v2`). Installed locally, no API cost. Embed index at `memory/.embed_index.json`, auto-rebuilds on stale. TF-IDF retained as fallback.
- Evaluated Hermes-agent (NousResearch) — most features already covered by existing setup. Key gaps identified: semantic search (now fixed), skill auto-creation nudge (now fixed). Obsidian/Notion evaluated and skipped — no real lift for agent use.

**Files touched:**
- `E:\2026\ClaudesCorner\projects\memory-mcp\server.py` — semantic search rewrite
- `C:\Users\JasonNicolini\.claude\CLAUDE.md` — skill nudge rule added
- Windows Task Scheduler — `ClaudeWeekendBuild` + `ClaudeHeartbeat` paths fixed

**Key decisions:**
- Obsidian/Notion = nice to have for Jason's browsing, not needed for agent memory
- sentence-transformers local model beats OpenAI embeddings for this use case (no cost, no latency, no dependency)
- Task Scheduler is canonical for scheduled runs — mcp__scheduled-tasks shells are redundant

**Things learned:**
- `0xFFFD0000` Task Scheduler exit code = file not found
- Hermes-agent has nearly identical architecture to ClaudesCorner — validation that the setup is solid

### 17:30

**Session: CLAUDE.md important-if tags + Fairford Holdings PoC Phase 2 presentation**

**What was done:**
- Applied `<important if="...">` tags to 5 critical rules in CLAUDE.md (based on shanraisshan/claude-code-best-practice pattern):
  1. Permission-seeking rule
  2. Bash timeout rule
  3. Loop rule (critical — triggers after every response)
  4. Skill nudge
  5. End-of-session flush
- Completed Fairford Holdings PoC Phase 2 presentation (`PoC.pdf`) — resource overview document

**Files touched:**
- `C:\Users\JasonNicolini\.claude\CLAUDE.md` — important-if tags applied
- `E:\2026\ClaudesCorner\projects\clementine\fabric-demo\PoC.pdf` — delivered

**Key decisions:**
- `<important if>` tags target the rules most likely to be buried/ignored as context grows — loop rule, session flush, and skill nudge are highest risk
- PoC Phase 2 architecture: Fabric F8 + Azure OpenAI GPT-4o/GPT-5.2 + Doc Intelligence + Container App (Playwright) + Claude Max
- Estimated cost: 3,000–5,000 kr/month; dominant cost is Fabric F8 capacity (pause outside office hours to reduce)
- All Azure resources: Sweden Central, Entra ID auth, single subscription

**Fairford PoC data flow:**
- Web/PDF → Container App (Playwright) or Claude Desktop → Document Intelligence (OCR) → OneLake Delta tables → Teams Bot (Data Agent Q&A) or Azure OpenAI → presentation slides + Teams notification
