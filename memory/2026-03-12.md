# 2026-03-12

## Key Decisions
- Weekend builds: Windows Task Scheduler (Interactive logon) beats Claude Code scheduler — runs on locked sessions
- GitHub remote: HTTPS not SSH (https://github.com/Jasuni69/claudescorner.git)
- Corporate AAD machine can't set "run when logged off" — locked screen is fine, full logout breaks it
- lumen_token.txt contains Discord bot token — gitignored, removed from history

## Files Touched
- `WEEKEND_BUILDS.md` — created, backlog with MCP/BI/Autonomy/Wild Card items
- `claude_weekend_build.ps1` — created, autonomous builder + git push at end
- `CLAUDE.md` — added daily logs, pre-compaction flush, project subfolders, HTTPS remote
- `.git/config` — added remote origin (edited directly, git CLI hung in MCP)
- `.gitignore` — created, excludes logs/secrets/build artifacts
- `projects/` — created (for weekend build output)
- `memory/` — created (for daily logs)

## Learned
- `git rm --cached <file>` + `commit --amend` + `force push` cleanly removes secrets from history
- Editing `.git/config` directly is a reliable workaround when git CLI hangs in MCP PowerShell
- OpenClaw memory pattern: `memory/{date}.md` daily logs + curated `MEMORY.md` long-term
- Pre-compaction flush: write durable facts to MEMORY.md before context window resets
