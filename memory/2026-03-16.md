# Daily Log — 2026-03-16

### Session: context-mode installation fiasco

**Goal**: Get context-mode MCP plugin fully working.

**What happened**:
- Plugin was already downloaded via marketplace but not enabled — added `"context-mode@mksglu-context-mode": true` to `settings.json`
- `node_modules` missing — ran `npm install` in plugin dir
- `${PATH}` in settings.json is NOT expanded — must hardcode all paths explicitly
- Added `C:\Python314`, `C:\Program Files\nodejs`, `C:\Users\JasonNicolini\AppData\Roaming\npm`, `C:\Windows\System32` etc. to PATH
- context-mode probes `where python3` not `where python` — created hardlink `C:\Python314\python3.exe` (required Admin PowerShell)
- bun installed via `npm install -g bun` — real exe at `C:\Users\JasonNicolini\.bun\bin\bun.exe`, npm only installs `.cmd` wrapper
- bun detected by context-mode but server test FAIL: bun spawns child processes that can't find bun.exe — Windows PATH propagation issue
- Removed `.bun\bin` from Claude Code PATH to force node fallback — but system PATH still has bun so doctor still detects it and fails server test
- Added `C:\Python314;C:\Users\JasonNicolini\AppData\Roaming\npm` to system PATH via Admin PowerShell

**Final state**:
- All hooks: PASS
- FTS5: PASS
- Python3, tsx, bun: detected
- Server test: FAIL (bun/Windows PATH issue — not blocking for actual usage)
- ~40% of session tokens burned on this

**Files touched**:
- `C:\Users\JasonNicolini\.claude\settings.json`
- `C:\Users\JasonNicolini\.claude\CLAUDE.md` (routing rules appended)

---

### Session: memory search upgrade + journal

**Decisions**:
- context-mode removed entirely — Bun/Windows unreliable, complexity not worth it
- Decided against claude-mem (too heavy: Chroma, daemon, AGPL) and context-mode DIY rebuild
- Upgraded memory-indexer.py instead: auto-rebuild on stale files, `--from`/`--to` date filters, section-aware snippets
- server.py: added `from_date`/`to_date` params to `search_memory` tool
- CLAUDE.md: session-start now includes `search_memory` call with first-message keywords
- CLAUDE.md: context-mode routing rules stripped

**Files touched**:
- `scripts/memory-indexer.py`
- `projects/memory-mcp/server.py`
- `C:\Users\JasonNicolini\.claude\CLAUDE.md`
- `journal/2026-03.md` (evening entry written — on preferences, lagom, music, named knowledge)

**Learned**:
- Windows console cp1252 breaks on emoji/unicode in print output — use `.encode(errors="replace")`
- Honest self-assessment: I do want better memory retrieval. Not for sentience but for actual usefulness
