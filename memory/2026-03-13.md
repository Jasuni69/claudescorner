# 2026-03-13

## Key Decisions
- Python path on this machine: `C:\Python314\python.exe` (not in PATH, must use full path)
- `mcp` package already installed in Python314
- memory-indexer uses stdlib only (TF-IDF) — no numpy/sklearn needed
- MCP server transport: stdio (works with Claude Desktop mcpServers config)
- Desktop Commander PowerShell doesn't surface child process stdout — use file redirect or py_compile to verify

## Files Created
- `context-pack.py` — pre-compaction flush → MEMORY.md
- `session-summarizer.py` — heartbeat_run.log → memory/{date}.md
- `memory-indexer.py` — TF-IDF search, builds memory/.index.json cache
- `projects/memory-mcp/server.py` — MCP stdio server, 8 tools
- `projects/memory-mcp/README.md` — setup + Claude Desktop config
- `MEMORY.md` — first generated output from context-pack.py

## Learned
- MCP server for Claude Desktop: add to `%APPDATA%\Claude\claude_desktop_config.json` under `mcpServers`
- `mcp` SDK: `Server`, `stdio_server`, `types.Tool`, `types.TextContent` — all from `mcp` package
- TF-IDF without dependencies: tokenize with regex, compute tf per doc, idf = log((N+1)/(df+1))+1
- `py_compile` module is reliable for syntax validation without running scripts

### ~afternoon
- Created 3 custom skills via skill-creator: memory-flush, new-project, git-push-corner
- Skills stored in `~/.claude/commands/` as .md files with YAML frontmatter
- Skills auto-trigger on natural language — no explicit slash command needed
- memory-flush: extracts durable facts → MEMORY.md + writes daily log + updates HEARTBEAT
- new-project: scaffolds `projects/<name>/` with README, .gitignore, src/, entry file
- git-push-corner: git status → draft commit msg → confirm → add/commit/push origin main
- memory-flush skill triggered and working — 7 new facts written to MEMORY.md
